setCanvasSize(600, 600);
float size = 0.001;
setPenSize(size);
setScale(0, 100);


function magic(Circle c){
    for(int i = 0 ; i < 80 ; i = i + 1){
        c.radius = c.radius + 1;
            c.draw();
            wait(20);
    }
}

setPenSize(0.01);

Point a(50, 50);
a.draw();
