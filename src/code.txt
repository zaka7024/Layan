setCanvasSize(600, 600);
float size = 0.001;
setPenSize(size);
setScale(0, 100);


function magic(Circle c){
    for(int i = 0 ; i < 80 ; i = i + 1){
        c.radius = c.radius + 1;
            c.draw();
            wait(20);
    }
}

int x [10];
int y = x[0];
x[0] = 6;

// TODO:: array member assignment
